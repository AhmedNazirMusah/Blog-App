<%= render "shared/user"%> 

<div class="user-bio">
  <h3>Bio:</h3>
  <p>
    <%= @single_user.bio %>
  </p>
</div>

<div>
  <% @user_posts.each.with_index(1) do |post, index|%>

    <div class="post-card box">
      <h3><%= post.title %></h3>
      <p>Post <%= index %></p>
      <p><%= post.text %></p>
      <span>
        Comments: <%= post.comments_counter %>
        Likes: <%= post.likes_counter %>
      </span>
    </div>
   
</div>
<% end %>
<%= link_to "See all Posts", users_posts_path(@single_user.id)%>